import React from 'react';
import ReactDOM from 'react-dom';
import Cashflow from './cashflow';

document.addEventListener("DOMContentLoaded", () => {
	const root = document.getElementById("root");
	ReactDOM.render(<Cashflow/>, root);
});
